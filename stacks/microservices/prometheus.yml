global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # Add your services here, e.g.:
  # - job_name: 'my-service'
  #   static_configs:
  #     - targets: ['my-service:8080']
